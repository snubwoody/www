---
import { ArrowUpRight } from "@lucide/astro";
---

<section class="flex max-md:flex-col px-20 md:px-64 py-128 gap-32 md:gap-64">
    <header data-observe class="landing-page-heading">
        <h2 data-observe>Currently working on</h2>
    </header>
    <div class="flex-1 space-y-16">
        <a href="https://www.halide.net" class="halide-link">
            <h3>Halide</h3>
            <ArrowUpRight/>
        </a>
        <p>
            A website where I publish in-depth courses and step-by-step guides, designed to make complex topics approachable and practical.
        </p>
    </div>
</section>

<style>
    .halide-link{
        display: flex;
        gap: 12px;
        width: fit-content;
        align-items: center;
        border-bottom: 1px solid var(--color-text-heading);
    }
    h2{
        opacity: 0;
        translate: -10% 0;
        transition: all 1s;

        &[data-visible="true"]{
            opacity: 1;
            translate: 0;
        }
    }

    li{
        opacity: 0;
        translate: 0 50%;
        transition: all 1s;

        &[data-visible="true"]{
            opacity: 1;
            translate: 0;
        }
    }
</style>
